info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "email":"juanma25051@gmail.com",
          "password":"Juanma123*"
          }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.body;

        // Guardar variable global
        bru.setEnvVar("JWT", data.accessToken);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
