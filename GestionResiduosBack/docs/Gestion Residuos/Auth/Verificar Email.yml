info:
  name: Verificar Email
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/auth/verify?token=TU_TOKEN_DE_VERIFICACION
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

