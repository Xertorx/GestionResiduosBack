info:
  name: Listar reportes
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:8080/api/reports
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
