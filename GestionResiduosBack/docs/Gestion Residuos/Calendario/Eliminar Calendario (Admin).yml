info:
  name: Eliminar Calendario (Admin)
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8080/api/schedules/1
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Elimina un calendario de recolección. Solo ADMINISTRADOR.
  Reemplazar 1 con el ID del calendario.

