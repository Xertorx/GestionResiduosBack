info:
  name: Listar Contenidos
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/api/v1/education
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
