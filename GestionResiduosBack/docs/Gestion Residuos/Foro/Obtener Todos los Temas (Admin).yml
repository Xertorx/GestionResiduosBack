info:
  name: Obtener Todos los Temas (Admin)
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/forum/topics/all
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Obtiene todos los temas del foro (activos e inactivos). Solo ADMINISTRADOR.


