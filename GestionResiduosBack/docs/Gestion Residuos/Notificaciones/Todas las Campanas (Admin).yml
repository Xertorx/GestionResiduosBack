info:
  name: Todas las Campanas (Admin)
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/notifications/campaigns
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Obtiene todas las campañas (activas, inactivas, finalizadas). Solo ADMINISTRADOR.

