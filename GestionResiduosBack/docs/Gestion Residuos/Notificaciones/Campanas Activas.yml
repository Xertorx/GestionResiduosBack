info:
  name: Campanas Activas
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/notifications/campaigns/active

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Obtiene las campañas ambientales activas vigentes (fecha actual entre startDate y endDate).
  No requiere JWT (público).

