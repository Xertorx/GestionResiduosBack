info:
  name: Obtener todos los usuarios
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/user/all
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
