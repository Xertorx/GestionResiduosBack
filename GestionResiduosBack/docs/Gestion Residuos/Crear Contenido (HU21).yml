info:
  name: Crear Contenido (HU21)
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseUrl}}/api/v1/education"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
