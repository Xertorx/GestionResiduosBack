info:
  name: Obtener Ecopuntos por Barrio
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/ecopoints/neighborhood/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

