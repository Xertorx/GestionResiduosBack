info:
  name: Eliminar Ecopunto
  type: http
  seq: 13

http:
  method: DELETE
  url: http://localhost:8080/api/ecopoints/1
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

