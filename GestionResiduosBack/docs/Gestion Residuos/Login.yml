info:
  name: login
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/auth/login
  body:
    type: json
    data: |-
      {
        "email": "garciadiego96@hotmail.com",
        "password": "Diego123*"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
