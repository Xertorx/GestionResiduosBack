info:
  name: Obtener mis reportes
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/reports/my-reports
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
