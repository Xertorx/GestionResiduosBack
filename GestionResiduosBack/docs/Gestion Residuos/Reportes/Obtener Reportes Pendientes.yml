info:
  name: Obtener Reportes Pendientes
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/reports/pending
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

