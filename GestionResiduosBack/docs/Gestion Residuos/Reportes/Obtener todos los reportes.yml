info:
  name: Obtener todos los reportes
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/reports
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
