info:
  name: Obtener Estadisticas
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8080/api/reports/statistics
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

