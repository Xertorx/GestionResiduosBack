info:
  name: Obtener Reportes por Tipo
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/reports/type/punto_critico
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

