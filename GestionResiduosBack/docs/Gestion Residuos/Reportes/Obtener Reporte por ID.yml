info:
  name: Obtener Reporte por ID
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/reports/1
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

