info:
  name: Obtener Categoria por ID
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/report-categories/1
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

