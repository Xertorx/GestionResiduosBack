info:
  name: Crear Contenido Educativo
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8080/api/v1/education
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
